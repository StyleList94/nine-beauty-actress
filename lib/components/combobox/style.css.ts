import { style } from '@vanilla-extract/css';

export const comboboxTrigger = style({
  width: '180px',
  justifyContent: 'space-between',
});

export const comboboxContent = style({
  width: '180px',
  padding: 0,
});

export const comboboxCheckIcon = style({
  marginLeft: 'auto',
});

export const comboboxCheckIconHidden = style({
  marginLeft: 'auto',
  opacity: 0,
});

export const comboboxChevronIcon = style({
  opacity: 0.5,
});
